Audit-Management-System
```
A simple audit management system application that allows to manage your audits by adding them, updating their status or deleting them from your system.

Download the source code and run npm install and then npm start to run the application. NodeJS, Express and MongoDB have been used for backend.